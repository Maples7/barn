doctype html
html(lang='en')
  head
    title= pageTitle
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    meta(http-equiv='X-UA-Compatible' content='ie=edge')
    link(rel='stylesheet' href='css/main.css')
  body
    div(class='container')
      div(class='left-section')
        div(class='head-pic')
          img(src='image/head.jpg')
        div(class='module')
          h1(class='title') PROFILE
          div(class='subtitle') LOREEER
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
        div(class='module')
          h1(class='title') PROFILE
          div(class='subtitle') LOREEER
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
        div(class='module')
          h1(class='title') PROFILE
          div(class='subtitle') LOREEER
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
        div(class='module')
          h1(class='title') PROFILE
          div(class='subtitle') LOREEER
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
        div(class='module')
          h1(class='title') PROFILE
          div(class='subtitle') LOREEER
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
      div(class='right-section')
        div(class='main-title')
          h1(class='title') JANE<br/>HARRISON
          h3(class='subtitle') Creative Director
        div(class='module')
          h1(class='title') WORK EXPERIENCE
          div(class='subtitle')
            span(class='company') Company Name<br/>
              span(class='job-position') Job Position
            span(class='time') 2011&nbsp;-&nbsp;2012
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
            ul(class='list')
              li A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
              li A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
          div(class='subtitle')
            span(class='company') Company Name<br/>
              span(class='job-position') Job Position
            span(class='time') 2011&nbsp;-&nbsp;2012
          div(class='content') A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
            ul(class='list')
              li A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
              li A resume/CV generator, parsing information from YAML file and output a static website which you can deploy on the Github Pages. Exactly like Hexo for generating resume static website.
        div(class='module')
          h1(class='title') SKILL & EXPERTIZE
        div(class='skill-list')
          div(class='skill')
            div(class='skill-name') HTML
            div(class='skill-bar') 
              span(class='value' style='width: 50%')
          div(class='skill')
            div(class='skill-name') CSS
            div(class='skill-bar') 
              span(class='value' style='width: 40%')
          div(class='skill')
            div(class='skill-name') JavaScript
            div(class='skill-bar') 
              span(class='value' style='width: 80%')
          div(class='skill')
            div(class='skill-name') Python
            div(class='skill-bar') 
              span(class='value' style='width: 70%')
